# LiveKit local development configuration

port: 7880
rtc:
  tcp_port: 7881
  udp_port: 7882
  stun_servers:
    - stun.l.google.com:19302
  use_external_ip: false

keys:
  devkey: secret

logging:
  level: debug

# Redis for room state persistence (local dev)
redis:
  address: localhost:6379
